name: Build ClearItemKhang

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Build with Maven
        run: mvn clean package -B

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ClearItemKhang-jar
          path: target/ClearItemKhang-*.jar
          retention-days: 7
